---
  class_name: "St2GenericWebhooksSensor"
  entry_point: "st2_generic_webhook_sensor.py"
  description: "Sensor which exposes an HTTP server and emits triggers for received webhooks"
  trigger_types:
    -
      name: "st2.webhook"
      description: "Triggers on received webhook"
      payload_schema:
        type: "object"
      parameters_schema:
        additionalProperties: false
        required:
          - "url"
        type: "object"
        properties:
          url:
            type: "string"
