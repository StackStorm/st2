---
  description: "Trigger type for generic webhooks."
  trigger_types:
    -
      name: "st2.webhook"
      description: "Triggers on received webhook"
      payload_schema:
        type: "object"
      parameters_schema:
        additionalProperties: false
        required:
          - "url"
        type: "object"
        properties:
          url:
            type: "string"
