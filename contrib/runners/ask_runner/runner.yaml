- description: A runner for launching python actions.
  enabled: true
  name: ask-action
  runner_module: ask_runner
  runner_parameters:
    schema:
      default:
        title: response_data
        type: object
        properties:
          continue:
           type: boolean
           description: Would you like to continue the workflow?
        required: ["continue"]
      description: A JSON schema that will be used to validate the response data
      type: object

