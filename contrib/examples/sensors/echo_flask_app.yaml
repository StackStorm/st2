---
  class_name: "EchoFlaskSensor"
  entry_point: "echo_flask_sensor.py"
  description: "Simple echo flask app sensor that echo's its input."
  trigger_types:
    -
      name: "echoflasksensor"
      description: "Echo flask sensor."
      payload_schema:
        type: "object"
        properties:
          payload:
            type: "object"
