version: '2.0'

examples.mistral-basic:
    type: direct
    input:
        - cmd
    output:
        stdout: $.stdout
    tasks:
        run-cmd:
            action: core.local cmd={$.cmd}
            publish:
                stdout: $.stdout
                stderr: $.stderr
