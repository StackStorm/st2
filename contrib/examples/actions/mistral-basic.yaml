name: 'examples.mistral-basic'
version: '2.0'
description: 'Basic mistral workflow example'
workflows:
    demo:
        type: direct
        input:
            - cmd
        tasks:
            run-cmd:
                action: core.local
                input:
                    cmd: $.cmd
                publish:
                    stdout: $.stdout
                    stderr: $.stderr
