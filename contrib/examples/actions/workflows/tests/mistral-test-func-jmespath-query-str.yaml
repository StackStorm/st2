version: '2.0'

examples.mistral-test-func-jmespath-query-str:
    description: A workflow for testing jmespath_query_str custom filter in mistral
    type: direct
    input:
        - input_str
        - input_query
    output:
        result_jinja: <% $.result_jinja %>
        result_yaql: <% $.result_yaql %>
    tasks:

        task2:
            action: std.noop
            publish:
                result_jinja: '{{ jmespath_query_str(_.input_str, _.input_query) }}'
                result_yaql: '<% jmespath_query_str($.input_str, $.input_query) %>'
