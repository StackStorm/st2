version: '2.0'

examples.mistral-ask-basic:
    description: A basic workflow that runs an arbitrary linux command.
    type: direct
    output:
        result: <% task(task1).result.response %>
    tasks:
        task1:
            action: core.ask
            input:
              tag: developers
              users:
                - testu
              schema:
                required:
                  - continue
                type: object
                properties:
                  continue:
                    type: boolean
                    description: This is a schema bassed in via action params
            on-complete:
              - task2

        task2:
            action: core.local
            input:
              cmd: date
