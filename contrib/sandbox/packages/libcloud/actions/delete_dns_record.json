{
    "name": "delete_dns_record",
    "runner_type": "run-python",
    "description": "Delete an existing DNS record.",
    "enabled": true,
    "entry_point": "delete_dns_record.py",
    "parameters": {
        "credentials": {
            "type": "string",
            "description": "Name of the credentials set (as defined in the config) to use.",
            "position": 0
        },
        "record_id": {
            "type": "string",
            "description": "ID of a record to delete",
            "position": 1
        }
    },
    "required_parameters": ["credentials", "record_id"]
}
