{
    "name": "puppet_cert_generate",
    "runner_type": "run-remote-script",
    "description": "Generate a certificate for a named client.",
    "enabled": true,
    "entry_point": "puppet_cert_generate.py",
    "parameters": {
        "host": {
            "type": "string",
            "description": "Hostname to use",
            "position": 0
        }
    },
    "required_parameters": ["host"]
}
