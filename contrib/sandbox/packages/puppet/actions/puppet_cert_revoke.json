{
    "name": "puppet_cert_revoke",
    "runner_type": "run-python",
    "description": "Revoke the certificate of a client.",
    "enabled": true,
    "entry_point": "puppet_cert_revoke.py",
    "parameters": {
       "environment": {
            "type": "string",
            "description": "Environment to use.",
            "position": 0
        },
        "host": {
            "type": "string",
            "description": "Host (or serial number) to revoke the certificate for.",
            "position": 1
        }
    },
    "required_parameters": ["environment", "host"]
}
