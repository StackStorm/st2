name: get_open_ports
runner_type: "python-script"
description: Retrieve open ports for a given host
enabled: true
entry_point: get_open_ports.py
parameters:
  ip_address:
    type: string
    description: "IP address of a host to query open ports from"
    required: true
