---
  description: "Check if a given branch in a ST2 Pack's Git repository should be auto deployed"
  enabled: true
  entry_point: 'check_pack_auto_deploy.py'
  name: "check_pack_auto_deploy"
  pack: "packs"

  parameters:
    branch:
      type: "string"
      description: "The branch in the ST2 Packs Git repository"
      required: true
    repository:
      type: "string"
      description: "The Git repository for the ST2 pack."
      required: true

  runner_type: "python-script"
