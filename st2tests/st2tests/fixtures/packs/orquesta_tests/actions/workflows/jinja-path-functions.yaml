version: 1.0

description: A basic workflow testing path related functions.

tasks:
  task1:
    action: core.noop

output:
  - basename: '{{ basename("/path/to/some/file.txt") }}'
  - dirname: '{{ dirname("/path/to/some/file.txt") }}'
