---
  name: "fail"
  description: "fail rule."
  trigger:
    type: "wolfpack.triggertype-1"
  criteria:
    trigger.k1:
      type: equals
      pattern: mismatch
  action:
    ref: "core.local"
    parameters:
      cmd: "echo \"fail\""
  enabled: true
